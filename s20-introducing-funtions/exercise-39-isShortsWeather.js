// Write function called isShortsWether
// It should accept a single number argument, which we will call temperature;
// If temperature is greater than or equal to 75 Fahrenheit, return true.
// Otherwise return false.
function isShortsWeather(temperature) {
    if (temperature >= 75) {
        return true;
    } else {
        return false;
    }
}